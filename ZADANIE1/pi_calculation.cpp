#include "pi_calculation.h"

void partial_sum(long start_idx, long end_idx, double step_size, double& result) {
    // w petli obliczyc sume wysokosci prostokatow czyli wartosc 4/(1+x*x)
    


    result = sum; // na koniec wynik przypisujemy do ostatniego argumentu
}
